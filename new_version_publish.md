# Terminal commands to publish new version
$ poetry lock        # Lock dependencies
$ poetry check       # Verify pyproject.toml is valid
$ poetry install     # Install dependencies
$ poetry build       # Build package distribution
$ poetry publish     # Publish to PyPI
